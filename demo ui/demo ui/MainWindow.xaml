<Window x:Class="WpfDemoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WPF Demo Screen" Height="600" Width="1000"
        Background="#F5F5F5">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="10,5,10,5"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Background" Value="#E0F7FA"/>
                <Setter Property="BorderBrush" Value="#006064"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#B2EBF2"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#80DEEA"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <StackPanel Grid.Column="0" VerticalAlignment="Top" Margin="0,10,0,0">
            <Button Content="Station Teaching" Click="MenuButton_Click" Tag="StationTeaching"/>
            <Button Content="Setting" Click="MenuButton_Click" Tag="Setting"/>
            <Button Content="I/O" Click="MenuButton_Click" Tag="IO"/>
            <Button Content="Mapper" Click="MenuButton_Click" Tag="Mapper"/>

            <StackPanel VerticalAlignment="Bottom" Margin="0,100,0,0">
                <Button Content="Others" Click="MenuButton_Click" Tag="Others"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Column="1" Margin="10" Background="White" BorderBrush="#CCCCCC" BorderThickness="1">
            <ContentControl x:Name="MainContentArea"/>
        </Border>

    </Grid>
</Window>